# Copy to .env and fill in. The server reads this; it is never bundled into the client.
#
# Get a key at https://console.anthropic.com/settings/keys
# Alternatively, run `ant auth login` and leave this blank — the SDK picks up
# the stored profile automatically with no code change.
ANTHROPIC_API_KEY=

# Optional overrides
PORT=8787
